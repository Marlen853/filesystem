# def add(x: int, y: int) -> imt:
#     print("переменная x равна", x)
#     print("переменная y равна", y)
#     return x + y

# print(add(10, 10))

# # lambda x, y: x + y

# add_lambda = lambda x, y: x + y
# print(add_lambda(10, 15))


# numbers = [2, 3, 4, 5]
# numbers_squared = map(lambda x: x**2, numbers)
# print(list(numbers_squared))


# numbers = [2, 3, 4, 5]
# numbers_squared = map(lambda x: x**2, numbers)
# numbers_divided_by_two = map(lambda x: x / 2, numbers_squared)
# print(list(numbers_divided_by_two))


# filter("func, collect")
# numbers = (3, 16, 25, 1, 4)
# odd_numbers = filter(lambda x: x % 2 != 0, numbers)


# words = ["banana", "six", "apple", "yellow", "something"]
# more_than_six_lettera = filter(lambda x: len[x] > 6, words)
# print(list(more_than_six_lettera))

